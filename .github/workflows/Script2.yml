name: Windows RDP Workspace

on:
  workflow_dispatch:

jobs:
  rdp-runner:
    runs-on: windows-latest
    timeout-minutes: 360

    steps:
      - name: Install Tailscale
        shell: powershell
        run: |
          $installer = "$env:TEMP\tailscale.msi"
          Invoke-WebRequest -Uri "https://pkgs.tailscale.com/stable/tailscale-setup-latest-amd64.msi" -OutFile $installer
          Start-Process msiexec.exe -ArgumentList "/i", "`"$installer`"", "/quiet", "/norestart" -Wait
          Remove-Item $installer -Force

      - name: Connect Tailscale
        shell: powershell
        run: |
          & "C:\Program Files\Tailscale\tailscale.exe" up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} --hostname=gh-win-runner
          $ip = & "C:\Program Files\Tailscale\tailscale.exe" ip -4
          echo "TAILSCALE_IP=$ip" >> $env:GITHUB_ENV
          Write-Host "Tailscale connected. IP: $ip"

      - name: Enable RDP & Credentials
        shell: powershell
        run: |
          # Enable RDP
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force
          # Disable NLA
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0 -Force
          # Open Firewall
          netsh advfirewall firewall add rule name="Allow RDP" dir=in action=allow protocol=TCP localport=3389
          
          # Setup User
          $user = "rdpuser"
          $pass = "SecurePass123!"
          net user $user $pass /add
          net localgroup administrators $user /add
          
          # Restart Services
          Restart-Service -Name "TermService" -Force

      - name: Keep Alive Loop
        shell: powershell
        run: |
          Write-Host "=========================================="
          Write-Host "RDP is ready. Connect via Tailscale IP: $env:TAILSCALE_IP"
          Write-Host "User: rdpuser | Pass: SecurePass123!"
          Write-Host "=========================================="
          # Infinite sleep to keep the runner alive
          while ($true) { Start-Sleep -Seconds 60 }
